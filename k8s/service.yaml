apiVersion: v1
kind: Service
metadata:
  name: mlops-diabetes-service
spec:
  type: LoadBalancer
  selector:
    app: diabetes-predictor
  ports:
    - protocol: TCP
      port: 80
      targetPort: 8501
